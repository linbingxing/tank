server:
  port: 9850

spring:
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.10.52:8848
  security:
    user:
      name: admin
      password: admin

#开启端点
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS